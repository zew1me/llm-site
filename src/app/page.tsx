export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="hero">
        <p className="eyebrow">Project Bootstrap</p>
        <h1>Next.js site scaffolded and ready to shape.</h1>
        <p className="lede">
          This starter keeps the initial surface area small so the real site can
          grow without first undoing a bulky template.
        </p>
      </section>
    </main>
  );
}
